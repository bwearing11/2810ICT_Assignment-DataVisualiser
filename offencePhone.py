import matplotlib
import wx
import openpyxl

def offencePhone():
    lst = []
    x = input("Enter a date range in the format: DD/MM/YY")             #this will be changed for the final version to
                                                                        # accept a date range as an argument
